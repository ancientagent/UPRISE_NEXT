# UPRISE_NEXT CLI Entry Point

Read `AGENTS.md` first; it is the primary agent entry point and routes to the
minimum current authority for the assigned lane.

CLI agents are bounded executors. Before starting or finishing write-enabled
work, follow `.pm/EXECUTOR_CONTRACT.md`. A meaningful task is not complete until
its factual check-in has been committed and pushed with the scoped work.
