# SLICE-AUTO-447A — Supervisor backoff jitter guardrails pack 6

## Scope
- Lane D automation only.
- Supervisor jitter guardrail diagnostics hardening only.

## Changes
- Updated `scripts/reliant-supervisor.mjs`:
  - Added `intervalConfig.maxSleepMs` (`intervalMs + intervalJitterMs`) for deterministic upper-bound loop timing visibility.
- Updated `scripts/reliant-supervisor.test.mjs`:
  - Added assertion for `maxSleepMs` in clamped interval scenario.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text
[verify output unavailable: runtime log removed during destructive clean]
```
