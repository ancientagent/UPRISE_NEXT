# SLICE-AUTO-386A — Claim refusal code normalization pack 4

## Scope
- Lane D automation only.
- Normalize claim result/refusal code surface across queue claim states.

## Changes
- Updated `scripts/reliant-slice-queue.mjs` (`claim`):
  - Added `resultCode` alongside existing `refusalCode` for all refusal outputs.
  - Added `resultCode: "claimed_new_task"` for successful fresh claims.
  - Kept existing exit codes and refusal semantics unchanged.
- Updated `scripts/reliant-slice-queue.test.mjs`:
  - Added assertions for `resultCode` on claim success and claim refusal paths.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
