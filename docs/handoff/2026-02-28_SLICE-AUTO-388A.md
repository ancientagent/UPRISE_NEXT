# SLICE-AUTO-388A — Runtime integrity diagnostics pack 4

## Scope
- Lane D automation only.
- Extend runtime checksum diagnostics metadata only, without queue transition behavior changes.

## Changes
- Updated `scripts/reliant-slice-queue.mjs` runtime status diagnostics:
  - Added `runtime.checksumAlgorithm` (currently `sha256` when present).
  - Added `runtime.checksumMode` (`none`, `explicit`, `invalid_runtime_auto`).
  - Preserved existing checksum and parse error semantics.
- Updated `scripts/reliant-slice-queue.test.mjs`:
  - Added assertions for checksum metadata in normal, explicit-checksum, and invalid-runtime paths.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
