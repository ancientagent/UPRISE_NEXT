# SLICE-AUTO-445A — Queue summary drift diagnostics pack 6

## Scope
- Lane D automation only.
- Deterministic queue status drift diagnostics extension only.

## Changes
- Updated `scripts/reliant-slice-queue.mjs`:
  - Added `summarySanity.severityRank` numeric mapping (`none=0`, `medium=1`, `high=2`, `critical=3`).
- Updated `scripts/reliant-slice-queue.test.mjs`:
  - Added assertions for `severityRank` in no-drift and drift scenarios.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text
[verify output unavailable: runtime log removed during destructive clean]
```
