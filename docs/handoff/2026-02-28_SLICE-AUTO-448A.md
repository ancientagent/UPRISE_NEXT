# SLICE-AUTO-448A — Runtime integrity diagnostics pack 6

## Scope
- Lane D automation only.
- Runtime diagnostics metadata hardening only.

## Changes
- Updated `scripts/reliant-slice-queue.mjs` runtime status diagnostics:
  - Added deterministic `runtime.lineCount` from raw runtime payload content.
- Updated `scripts/reliant-slice-queue.test.mjs`:
  - Added assertions for `lineCount` presence in runtime status outputs.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text
[verify output unavailable: runtime log removed during destructive clean]
```
