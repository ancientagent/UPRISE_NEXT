# SLICE-AUTO-414A — Supervisor ownership diagnostics pack 5

## Scope
- Lane D automation only.
- Ownership diagnostics/operator cues hardening only.

## Changes
- Updated `scripts/reliant-supervisor.mjs`:
  - Added deterministic `ownershipHealth.failureCode` values:
    - `stale_runtime`
    - `missing_runtime`
    - `invalid_runtime_payload`
    - `runtime_owner_mismatch`
    - `null` when healthy.
- Updated `scripts/reliant-supervisor.test.mjs`:
  - Added assertions for `failureCode` in healthy and stale-runtime cases.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
