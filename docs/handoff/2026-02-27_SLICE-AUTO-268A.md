# SLICE-AUTO-268A — Claim/complete race retry policy tightening

## Summary
- Added one-shot retry policy support to queue transitions via `--retry-ms` in `scripts/reliant-slice-queue.mjs`.
- Applied retry handling to transient race paths in `claim`, `complete`, and `block` while preserving strict `--task-id` guard semantics.
- Added deterministic retry diagnostics (`retry-attempted=true`) on terminal race errors after retry.
- Extended queue tooling test coverage for retry-attempted diagnostics.

## Files Changed
- `scripts/reliant-slice-queue.mjs`
- `scripts/reliant-slice-queue.test.mjs`
- `docs/CHANGELOG.md`

## Command Outputs

### 1) Queue tooling tests
Command:
```bash
node scripts/reliant-slice-queue.test.mjs
```
Output:
```text
(no stdout/stderr; exit 0)
```

### 2) Required verify command (exact)
Command:
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```
Output:
```text
[docs:lint] ✅ docs:lint passed
[canon:lint] OK: Checked 10 canon markdown files
✅ Web-Tier Contract Guard: No violations detected!
✅ Build succeeded: All checks passed!
> api@1.0.0 typecheck ... tsc --noEmit
> web@1.0.0 typecheck ... tsc --noEmit
```
