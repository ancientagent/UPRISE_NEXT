# SLICE-AUTO-295A — Queue summary recompute sanity check hook

## Summary
- Added `summarySanity` diagnostics to queue `status` output in `scripts/reliant-slice-queue.mjs`.
- Status now compares optional persisted queue summary against recomputed actual summary and emits deterministic drift entries.
- Added queue-tooling tests for both no-summary and persisted-summary-drift scenarios.

## Command Outputs
- `node scripts/reliant-slice-queue.test.mjs` -> exit 0 (pass)
- Required verify command -> docs:lint, infra-policy-check, api/web typecheck all passed.
