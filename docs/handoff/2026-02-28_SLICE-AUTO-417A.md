# SLICE-AUTO-417A — Supervisor backoff jitter guardrails pack 5

## Scope
- Lane D automation only.
- Supervisor jitter diagnostics/guardrail hardening only.

## Changes
- Updated `scripts/reliant-supervisor.mjs`:
  - Added `intervalConfig.jitterCycleLength` (`intervalJitterMs + 1`, minimum 1) for deterministic loop jitter cycle introspection.
- Updated `scripts/reliant-supervisor.test.mjs`:
  - Added assertion for `jitterCycleLength` in clamped interval config.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
