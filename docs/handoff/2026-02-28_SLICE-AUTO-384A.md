# SLICE-AUTO-384A — Supervisor ownership diagnostics pack 4

## Scope
- Lane D automation only.
- Additive ownership diagnostics/cues only; no queue semantic changes.

## Changes
- Updated `scripts/reliant-supervisor.mjs`:
  - `ownershipHealth` now includes `severity` (`none|medium|high|critical`) and `requiresIntervention`.
  - Existing states/hints/operator cues preserved.
- Updated `scripts/reliant-supervisor.test.mjs`:
  - Added assertions for `severity` and `requiresIntervention` in healthy and stale-runtime scenarios.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
