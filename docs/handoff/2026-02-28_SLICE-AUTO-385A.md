# SLICE-AUTO-385A — Summary drift detector pack 4

## Scope
- Lane D automation only.
- Extend deterministic queue summary drift diagnostics only.

## Changes
- Updated `scripts/reliant-slice-queue.mjs` status output:
  - Added `summarySanity.driftDeltas` keyed by drifted summary field with `{ declared, actual, delta }`.
  - Existing `drift`, `driftKeys`, and severity behavior preserved.
- Updated `scripts/reliant-slice-queue.test.mjs`:
  - Added assertions for `driftDeltas.total` and `driftDeltas.queued`.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
