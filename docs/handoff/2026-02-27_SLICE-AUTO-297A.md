# SLICE-AUTO-297A — Supervisor loop backoff and jitter guardrails

## Summary
- Added deterministic bounded jitter support to supervisor watch loops via `--interval-jitter-ms`.
- Sleep interval now uses bounded deterministic offset per cycle to reduce synchronized collision risk across simultaneous lane watchers.
- Added supervisor test coverage for jitter bounds determinism.

## Command Outputs
- `node scripts/reliant-supervisor.test.mjs` -> exit 0 (pass)
- Required verify command -> docs:lint, infra-policy-check, api/web typecheck all passed.
