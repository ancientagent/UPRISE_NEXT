# SLICE-AUTO-416A — Claim result code normalization pack 5

## Scope
- Lane D automation only.
- Normalize transition result code surface for operator tooling.

## Changes
- Updated `scripts/reliant-slice-queue.mjs`:
  - Added `resultCode` to transition outputs:
    - `complete` success: `completed`
    - `complete` idempotent path: `complete_idempotent`
    - `block` success: `blocked`
  - Existing `claimed/refusalCode` and exit semantics preserved.
- Updated `scripts/reliant-slice-queue.test.mjs`:
  - Added assertions for new transition `resultCode` values.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
