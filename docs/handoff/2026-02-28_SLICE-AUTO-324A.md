# SLICE-AUTO-324A — Supervisor lane ownership diagnostics pack 2

## Summary
- Extended `scripts/reliant-supervisor.mjs` with ownership health classification.
- Added per-lane `ownershipHealth` output (`state`, `hints`) derived from runtime ownership and in-progress count.
- Added deterministic stale-state hint signals (`clear_runtime`, `recreate_runtime`, `runtime_clean_then_reclaim`) for triage.
- Kept queue transition semantics unchanged.

## Command Outputs
- `node scripts/reliant-supervisor.test.mjs` -> exit 0
- Required verify command -> docs:lint, infra-policy-check, api/web typecheck all passed.
