# SLICE-AUTO-267A — Queue status drift detector for lane supervisors

## Summary
- Added drift detector logic in `scripts/reliant-supervisor.mjs` for summary mismatches, invalid statuses, impossible ownership states, and runtime/in-progress inconsistencies.
- Added deterministic drift diagnostics output and `--fail-on-drift` exit behavior (exit code `4`).
- Added `--lanes-json` support for deterministic lane-scoped supervisor runs.
- Added `scripts/reliant-supervisor.test.mjs` covering drift detection and fail-on-drift exit path.

## Files Changed
- `scripts/reliant-supervisor.mjs`
- `scripts/reliant-supervisor.test.mjs`
- `docs/CHANGELOG.md`

## Command Outputs

### 1) Supervisor drift tests
Command:
```bash
node scripts/reliant-supervisor.test.mjs
```
Output:
```text
(no stdout/stderr; exit 0)
```

### 2) Required verify command (exact)
Command:
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```
Output:
```text
[docs:lint] ✅ docs:lint passed
[canon:lint] OK: Checked 10 canon markdown files
✅ Web-Tier Contract Guard: No violations detected!
✅ Build succeeded: All checks passed!
> api@1.0.0 typecheck ... tsc --noEmit
> web@1.0.0 typecheck ... tsc --noEmit
```
