# SLICE-AUTO-444A — Supervisor ownership diagnostics pack 6

## Scope
- Lane D automation only.
- Ownership diagnostics/operator cues hardening only.

## Changes
- Updated `scripts/reliant-supervisor.mjs`:
  - Added deterministic `ownershipHealth.recoveryPriority` (0 healthy, 2 stale-runtime, 3 missing/invalid runtime, 4 owner mismatch).
- Updated `scripts/reliant-supervisor.test.mjs`:
  - Added assertions for `recoveryPriority` in healthy/stale-runtime scenarios.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text
[verify output unavailable: runtime log removed during destructive clean]
```
