# SLICE-AUTO-389A — Runbook copy-safe command hardening pack 4

## Scope
- Lane D automation only.
- Docs-only command-copy hardening in the Reliant orchestrator runbook.

## Changes
- Updated `docs/solutions/RELIANT_ORCHESTRATOR_RUNBOOK.md`:
  - Replaced remaining angle-bracket placeholders in manual claim/complete/block section with copy-safe quoted variables.
  - Updated optional `--task-id` guardrail note to quoted variable form.
  - Removed a malformed duplicate rollback-checkpoint section fragment that had placeholder text without valid command content.

## Verify Command (exact)
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```

## Command Output (exact)
```text

> web@1.0.0 typecheck /home/baris/UPRISE_NEXT/apps/web
> tsc --noEmit

```
