# SLICE-AUTO-328A — Runtime file integrity diagnostics pack 2

## Summary
- Extended runtime diagnostics in queue status with `sizeBytes` and `parseErrorKind` fields.
- Added parse error classification for invalid runtime payloads (`json_syntax` vs `runtime_io_or_shape`).
- Preserved queue transition behavior; diagnostics-only augmentation.

## Command Outputs
- `node scripts/reliant-slice-queue.test.mjs` -> exit 0
- Required verify command -> docs:lint, infra-policy-check, api/web typecheck all passed.
