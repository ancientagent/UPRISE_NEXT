# SLICE-AUTO-298A — Runtime file integrity checksum diagnostics

## Summary
- Added optional runtime integrity checksum diagnostics to queue status via `--runtime-checksum`.
- Status now includes `runtime.checksumSha256` when checksum mode is enabled, without changing transition behavior.
- Added queue-tooling tests for checksum presence and shape.

## Command Outputs
- `node scripts/reliant-slice-queue.test.mjs` -> exit 0 (pass)
- Required verify command -> docs:lint, infra-policy-check, api/web typecheck all passed.
