# SLICE-AUTO-266A — Runtime stale-lock recovery utility extension

## Summary
- Extended `scripts/reliant-runtime-clean.mjs` with deterministic dry-run diagnostics via `--dry-run`.
- Added cleanup result metadata fields: `dryRun`, `wouldClear`, `runtimeState` (`missing|present|invalid_json`).
- Added dedicated utility tests for missing/present/invalid runtime paths and dry-run behavior.

## Files Changed
- `scripts/reliant-runtime-clean.mjs`
- `scripts/reliant-runtime-clean.test.mjs`
- `docs/CHANGELOG.md`

## Command Outputs

### 1) Utility tests
Command:
```bash
node scripts/reliant-runtime-clean.test.mjs
```
Output:
```text
(no stdout/stderr; exit 0)
```

### 2) Required verify command (exact)
Command:
```bash
pnpm run docs:lint && pnpm run infra-policy-check && pnpm --filter api typecheck && pnpm --filter web typecheck
```
Output:
```text
[docs:lint] ✅ docs:lint passed
[canon:lint] OK: Checked 10 canon markdown files
✅ Web-Tier Contract Guard: No violations detected!
✅ Build succeeded: All checks passed!
> api@1.0.0 typecheck ... tsc --noEmit
> web@1.0.0 typecheck ... tsc --noEmit
```
