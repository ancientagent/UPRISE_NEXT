# SLICE-AUTO-325A — Queue summary drift sanity hook pack 2

## Summary
- Hardened queue `summarySanity` output with normalized `persisted` and `actual` snapshots.
- Added `isDrifting` and `severity` (`none|medium|high`) fields for deterministic operator diagnostics.
- Preserved transition behavior; diagnostics-only extension.

## Command Outputs
- `node scripts/reliant-slice-queue.test.mjs` -> exit 0
- Required verify command -> docs:lint, infra-policy-check, api/web typecheck all passed.
