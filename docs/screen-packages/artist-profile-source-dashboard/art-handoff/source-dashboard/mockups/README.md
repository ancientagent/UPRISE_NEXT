# Source Dashboard Mockups

Status: empty by policy
Package: `artist-profile-source-dashboard`
Screen/section: `source-dashboard`

This folder is reserved for working mockup drafts only if the user explicitly
approves storing specific mockup files in the repo.

Current rule:

- no PNG/JPG/mockup images are stored here;
- no Codex generated image cache files should be copied here;
- no unapproved design drafts should be committed;
- if a mockup is later stored, include a short approval note that names the
  user approval, source prompt, date, intended use, and whether the asset is a
  working draft or a candidate approved target.

Until approval exists, this README is the only intended file in `mockups/`.
